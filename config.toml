# Optional config file for Valence. Safe to delete — every key below has a
# built-in default, and this file is not required to run.
# Environment variables (prefixed with VALENCE_) take precedence over this file.
debug = false
extern_port = 3030
cache_url = "redis://127.0.0.1:6379"
cache_ttl_secs = 600
body_limit_bytes = 8192
